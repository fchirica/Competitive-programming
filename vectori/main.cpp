#include <stdio.h>

//1 = black
//0 = red

bool root = 1;

int main() {
    int q;
    scanf("%d", &q);


    while (q--) {
        char ch[3];
        scanf("%s", &ch);
        if (ch == "Qi")
    }
    return 0;
}
